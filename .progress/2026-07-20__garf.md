# Progress log – 2026-07-20 – garf

## Entry 1 – PF++-success investigation opened: probing + experiment plan (T0–T4)

- timestamp: 2026-07-20
- branch/commit: main (docs only, uncommitted)

- goal: mirror-image of the closed GARF root-cause arc — why does PF++
  (denoiser-only) form a semi-good vessel shape on the 9-piece Juglet, what
  differs vs GARF, what is the main success factor.

- probing results (from existing evidence, no new jobs):
  - PF++ Juglet deploy confirmed **denoiser-only** (`eval_juglet_deploy.slurm`,
    `verifier.max_iters=1`) — verifier/merge/Jigsaw machinery played no role.
  - Shared inputs audited: BOTH frameworks anchor the largest part, per-part
    recenter + unit-normalise, **both NeRF-embed per-part scale**
    (GARF `denoiser_transformer.py:156-177`), both 20 steps, both trained on
    BB everyday. → real differentiators are only (a) representation
    (dense fracture-pretrained PTv3 vs 25-token coarse VQVAE) and
    (b) objective (flow matching vs DDPM).
  - Re-read of Exp 9: PF++ layout is form-correct but **contact-empty**
    (0 contact band on all 18 mated pairs; 18/36 coarse-touching).
  - Key crossover surfaced: PF++ denoiser-only scores 21.5% PA / 65.8° on
    *fresh* Fractura ceramics (vs GARF 56.3% / 38.6°) yet "succeeds" on
    Juglet → its fallback output is a plausible vessel regardless of solving;
    GARF's fallback is incoherent. Frames front-runner hypothesis.
  - Spartan assets verified over SSH (PF++ deploy outputs, pc_data npz,
    juglet_adjacency, pair_chamfer harness, anchor-centered meshes).

- deliverable: `docs/notes/PFPP_JUGLET_SUCCESS_EXPERIMENT_PLAN.md` —
  hypotheses F1 (prior-driven form composition, front-runner) / F2 (genuine
  coarse-geometry perception) / F3 (wear-invariant representation) /
  F4 (objective) / F5 (residual viz flattery); experiments T0 (like-for-like
  no-GT layout panel incl. vessel-ness fit, CPU-only), T1 (sherd scramble /
  seed consistency / seam continuity), T2 (wear-invariance of outputs +
  VQVAE latents, mirror of Exp 10), T3 (36-pair oracle on PF++ with the
  Exp 6 instrument + gates), T4 (VQ-token shuffle/random/zero prior-dominance
  ablation); pre-registered gates + decision matrix → main-factor verdict.
  Cross-linked from JUGLET_ROOTCAUSE_FINDINGS.md.

- what's next:
  - T0 is CPU-only on saved outputs — can run without a GPU job.
  - GPU experiments (T4 → T1 → T3 → T2, ≈5–6 GPU-hours total) need user OK
    per workspace Slurm rule; scripts to implement per plan.

- blockers: none for T0; Slurm submissions await user approval.

## Entry 2 – PF++-success investigation EXECUTED and CLOSED (T0–T4)

- timestamp: 2026-07-20 (afternoon)
- jobs: T0 27650662/27651352/27651601; T4 27650827+27651278/27651602/27651351;
  T1/T3 27651277+27651279; T2 27651483; fix 27651794; T1c 27652278.

- verdict (full numbers in docs/notes/PFPP_JUGLET_SUCCESS_FINDINGS.md):
  **PF++ succeeds by curvature-coherent form composition over wear-invariant
  coarse features — it perceives per-sherd macro-curvature, not mating.**
  - T0: gap vs GARF is vessel-ness (profile 0.961 vs 0.719≈pile), not
    compactness (both compact); PF++ interpenetration 0.0000 (9/9 watertight).
  - T4: token corruption (shuffle/random/zero) collapses vessel to pile —
    form is token-driven, not hallucinated.
  - T1a: dup(9 copies of 1 sherd)=PERFECT vessel (1.000); mirror=0.928
    (mating impossible, form unaffected); foreign=0.759 (curvature coherence
    required). No mating structure needed.
  - T1b: form stable across 5 seeds (0.89–0.98) but arrangement re-drawn at
    random (127° ≈ 126.9° random expectation) — interchangeable slots.
  - T3: juglet pairs pass gates (0.026 vs 0.046, 1.72×) but vs PF++'s own
    layout = self-consistency; CONTROL (real GT, fresh ceramics) FAILS the
    1.25× gate (1.17×; GARF 1.61×) — no independent mating perception.
  - T2: output form wear-invariant within resample noise (0.939 fresh rerun
    vs 0.904 dewear); latent probe: wear cosine 0.811 ≈ resample null 0.823
    ≫ cross-part 0.284 — representation blind to the wear axis that moved
    GARF's fracture response 3.8× (Exp 14). Pose-level gates unusable: the
    resample null alone is 48°/0.27 (slot instability).
  - T1c: seam thickness/curvature continuity not significant vs permuted
    null (p=0.15/0.18) — no seam-level macro matching either.

- fixes along the way: PF++ ablation_mode hook in auto_aggl.py; converter
  frame mismatch diagnosed (May npz vs rebuilt hdf5 — invalidated first T2
  pass; redone same-build); latent probe needed FPS-center matching; control
  pair build needs GARF venv (conda env lacks h5py).

- implication recorded in findings: both models fail at contact level on
  worn sherds; PF++ layout = coarse init only; Exp 16 hybrid (curve-based
  pairing + PF++-style form prior + fracture refinement) is the path.
