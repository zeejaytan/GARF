# Progress log – 2026-07-18 – garf

## Entry 1 – designed + built Exp 14 (de-weathering sufficiency) & Exp 15 plan

- timestamp: 2026-07-18
- branch/commit: main @ 48c0624 (uncommitted work below)

- what changed:
  - Goal: design the decisive test that encoder blindness to worn breaks is the
    EXACT cause of the Juglet failure, and build the solution path to an actual
    Juglet reconstruction. Gap identified: Exp 10/10b prove only the forward
    direction (wear → blindness). Missing: SUFFICIENCY on the failing object —
    de-wear the Juglet, perception AND mating must return.
  - **`fracture_mesh_ops.sharpen_fracture_band_solo` (NEW):** inverse of the
    causally-validated mollifier wear model — surface unsharp mask
    `v' = v + s·w·(v − mollify(v))` on the fracture band. Band detected with
    the validated pose-free relief detector (PF++ poses give zero contact band,
    Exp 9, so physical detection is unusable — pose-free also means the
    transform deploys at inference on any scan). Complementarity of true mates
    preserved to first order; displacement clamped to the mollify radius.
    `region="offband"` variant for the specificity control.
  - **`scripts/exp14_dewear_sufficiency.py` (NEW):** `probe` (frozen-encoder
    fired% sweep over strengths, arms A/C) and `build` (de-weather every piece
    of a deploy/pairs HDF5 in place — shapes/faces/poses unchanged so all
    downstream tooling works verbatim).
  - **`slurm/exp14_dewear_sufficiency.slurm` (NEW):** full arc — arm A
    perception sweep (gate: juglet fired% ≥1.5%, baseline 0.57%, control 3.4%);
    arm C offband specificity (< 1.5× baseline); arm B decisive 3-seed pairwise
    chamfer oracle on de-weathered pairs (gate: true-mate median ≤0.045 AND
    mate/non separation ≥1.25×; baseline 0.070==0.073) + 9-piece de-weathered
    deploy GLB; optional co-adapt stacked arm (Exp 13 ckpt autodetect /
    COADAPT_CKPT); arm D control regression env-gated (RUN_CTRL_REGRESSION=1,
    gate ≤0.030). Chamfer scored against ORIGINAL geometry references so
    numbers stay comparable to Exp 6/12/13.
  - **Ledger updated** (JUGLET_ROOTCAUSE_EXPERIMENT_PLAN.md): Exp 14 gates
    pre-registered; Exp 15 (contingency solution) designed — Juglet-domain
    self-supervised FracSeg adaptation with relief-detector pseudo-labels
    (geometric, no circularity) + 1:4 synthetic replay, then merge encoder +
    Exp 13 denoiser co-adapt + re-score; overfit guards (9 sherds only):
    heavy augmentation + early stop on synth probe AUC ≥0.90.
  - **Local smoke test passed** (scratchpad): band-selective (top/bottom
    displacement 8:1), strength-monotonic, clamp respected, relief_p90
    0.19→0.81, offband mask inverts, mate complementarity preserved
    (gap drift 0.003 vs 0.013 displacement).
  - Interpretation table: A+B pass ⇒ cause proven sufficient AND the sharpener
    IS the remedy (inference-time de-weathering preprocessor, no retraining;
    integrate into rebuild_juglet_pipeline + deploy). A pass, B fail ⇒
    denoiser also lost domain competence — co-adapt arm disambiguates.
    A fail ⇒ Exp 15. Both fail ⇒ definitive: real weathered supervision
    required.

- what's next:
  - Commit + push, then (user OK) `./scripts/remote/pull_and_sbatch.sh
    slurm/exp14_dewear_sufficiency.slurm` (~2.5–4 h on gpu-a100).
  - Read arm A verdict first; arm B chamfer summaries:
    `logs/diagnostics/pair_chamfer_juglet_dewear_{base,coadapt}_<stamp>/summary.md`.
  - If SUFFICIENCY CONFIRMED: wire `--dewear-strength` into
    rebuild_juglet_pipeline.py as the production preprocessor and evaluate the
    9-piece GLB; else launch Exp 15.

- blockers:
  - Slurm submission awaiting user OK (workspace rule 3).

## Entry 2 – Exp 14 submitted + RESULT (job 27540860)

- timestamp: 2026-07-19 ~01:00
- branch/commit: main @ 8ae8cd9

- what changed:
  - User OK'd the run: committed/pushed 8ae8cd9, `pull_and_sbatch.sh` →
    job 27540860 (gpu-a100, ~2h queue + ~2h run).
  - **Arm A PASSED:** de-weathering restores encoder response — juglet fired%
    0.50→1.88% at strength 2.0 (gate ≥1.5%); inverted-U dose-response
    (s=3.0 → 1.07%, control 4.3→0.9%) = texture-class sweet spot.
  - **Arm C PASSED:** offband sharpening 0.22% — effect is band-specific.
  - **Arm B FAILED (both arms):** de-weathered pairs, 3-seed chamfer oracle:
    base 0.0687/0.0678 (true) vs 0.0696/0.0607 (non); co-adapt stack
    0.0663/0.0693 vs 0.0688/0.0638. No separation; gates ≤0.045 / ≥1.25×
    missed. Best pair again p0104 at 0.0291 (control quality).
  - **Verdict:** sufficiency by geometric inversion NOT achievable — firing
    the fracture head ≠ restoring mating; amplification cannot re-synthesize
    abrasion-destroyed micro-structure. Findings addendum + ledger updated.

- what's next:
  - Decision point: launch Exp 15 (Juglet-domain self-supervised FracSeg
    adaptation w/ relief pseudo-labels + synthetic replay, then merge +
    co-adapt + re-score; script not yet written) — distinguishes
    representation-mismatch (a) from information-loss (b). If 15 also fails,
    (b) stands: real weathered supervision or non-fracture cues required.

- blockers:
  - none.
