# Progress log – 2026-03-31 – garf

## Entry 1 – 14:25
- timestamp: 2026-03-31 14:25
- branch/commit: main @ 2d489c0

- what changed:
  - Rebuilt Python environment to match upstream: Python 3.12.3, PyTorch 2.8.0+cu128, flash-attn 2.8.3, spconv-cu126, torch-scatter
  - Built pytorch3d 0.7.7 from source via SLURM job (`build_pytorch3d.slurm`) to fix GLIBC_2.35 incompatibility — pre-built wheel required glibc 2.35, HPC has 2.34
  - Verified CUDA forward compatibility on H100 (driver 550 / CUDA 12.4 running cu128 code) — all GPU ops pass
  - Fixed upstream bug in `assembly/data/breaking_bad/weighted.py:185`: `data["meshes"]` KeyError during training — made meshes key conditional
  - Fixed upstream bug in `assembly/data/breaking_bad/module.py:145-151`: added `collate_fn` to `val_dataloader` — val samples with meshes crashed `default_collate`
  - Extracted feature extractor weights (402 params) from `output/GARF.ckpt` into standalone `output/feature_extractor.ckpt`
  - Ran successful 5-epoch smoke test on H100 with Fractura bone_synthetic (pig category): bf16-mixed, SDPA attention, no NaN, loss decreasing (vec_mse_loss=0.742)
  - Updated `CONVERGENCE_DIAGNOSIS.md` with environment rebuild results, data loading bug analysis, and smoke test metrics

- what's next:
  - none

- blockers:
  - none
