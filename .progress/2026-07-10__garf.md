# Progress log – 2026-07-10 – garf

## Entry 1 – 17:05
- timestamp: 2026-07-10 17:05
- branch/commit: main @ 2d489c0

- what changed:
  - Monitored and closed out Exp 7 (rim-erosion bridge, job 27038450, 1h36m) and
    Exp 8 (rim-oversampling remedy, job 27038543, 1h59m), both submitted earlier
    today.
  - **Exp 7 verdict: NOT confirmed.** Eroding the control ceramics' fracture
    bands degrades pairwise perception monotonically (true-mate chamfer/diag
    0.0229→0.0317, separation 1.61x→1.24x) but plateaus at less than half of
    Juglet's 0.070 failure level. Caveat: achieved band relief_p90 saturated at
    ~0.20, never reaching Juglet's 0.171 (mollifier stops carving once locally
    smooth) — the bridge undershot calibration. Worn rims are contributing, not
    sufficient. `logs/diagnostics/exp7_erosion_20260710_141409/summary.md`.
  - **Exp 8 verdict: no effect — remedy refuted.** Rim oversampling (frac
    0.35/0.60) moves Juglet true-mate median 0.0731→0.0694 (best −5%, below the
    pre-registered >10% bar), no mate/non separation emerges (0.92–1.02x; at
    0.60 mates score worse than non-mates). Control regression at 0.35 passes.
    The deficit is not point-starvation at the rim.
    `logs/diagnostics/exp8_rim_20260710_142521/summary.md`; 9-piece deploy GLBs
    at `logs/deploy/exp8_j9pc_20260710_142521_f{035,060}/` (unscored — pairwise
    gate already failed).
  - Updated `JUGLET_ROOTCAUSE_EXPERIMENT_PLAN.md` (2026-07-10 results entry in
    the hypothesis ledger) and `JUGLET_ROOTCAUSE_FINDINGS.md` (addendum:
    worn-rim story incomplete; revised remedy direction).

- what's next:
  - Cheap probes to isolate the remaining Juglet-specific factor: (a) rerun
    erosion bridge with larger mollify radius to break the relief_p90 plateau;
    (b) bound PF++ pseudo-GT label error by re-scoring Juglet pairs against a
    relaxed best-fit reference. Then decide on fine-tuning with
    erode_fracture_band augmentation (tempered expectations — eroded fresh
    ceramic is an imperfect Juglet proxy).

- blockers:
  - none

## Entry 2 – 17:35
- timestamp: 2026-07-10 17:35
- branch/commit: main @ 2d489c0

- what changed:
  - Designed, validated, and submitted both follow-up probes from Entry 1:
  - **Exp 7b (job 27043960, gpu-a100)** `exp7b_erosion_radius.slurm`: erosion
    kernel-radius sweep to break Exp 7's relief_p90 plateau. Root cause of the
    plateau: mollifier saturates at its fixed physical radius AND knn=48 caps
    the effective kernel at large radii. Added `--erode-kernel-frac` /
    `--erode-knn` to `build_control_pairs_hdf5.py`, `--x-label` to
    `summarize_erosion_bridge.py`. Arms: strength 1.0 × kernel_frac
    {0.10, 0.15, 0.20}, knn {192, 432, 768}; reuses exp7's scored 0.00/0.05
    bridge points.
  - **Exp 9 (job 27043961, sapphire CPU)** `exp9_pseudo_gt_bound.slurm` + new
    `scripts/juglet_pseudo_gt_bound.py`: bounds PF++ pseudo-GT label error in
    the 0.070 Juglet true-mate chamfer. Trimmed band-constrained ICP (keep=0.7)
    refines each true-mate reference pose; re-scores existing Exp 6/6b GLBs
    against the refined reference; control ceramics (real GT) as built-in
    validation. Smoke-tested on login node: band extraction OK, trimmed ICP
    drift on GT pairs ~0.008 with measured sampling-noise identity floor
    0.0046–0.0064 (reported in the summary so sub-floor drift isn't over-read).
  - Logged both submissions in `JUGLET_ROOTCAUSE_EXPERIMENT_PLAN.md`.

- what's next:
  - When jobs finish: read `logs/diagnostics/exp7b_radius_*/summary.md` and
    `logs/diagnostics/exp9_pseudogt_*/summary.md`; reconcile the two verdicts
    (full-depth wear sufficient? labels trustworthy?) and update
    JUGLET_ROOTCAUSE_FINDINGS.md; then decide fine-tuning vs pseudo-GT
    replacement.

- blockers:
  - none
