# Progress log – 2026-04-17 – garf

## Entry 1 – 14:53
- timestamp: 2026-04-17 14:53
- branch/commit: main @ 2d489c0

- what changed:
  - Created `Puzzlefusion/generate_oracle_matching_data.py` — assembly normalization + oracle matching data generation (EPS_CRITICAL=0.02, EPS_CORR=0.02, MIN_EDGE_CORR=3); outputs `data/pc_data_norm/<ds>/val/` and `data/matching_data_oracle/<ds>/`.
  - Fixed DataLoader collate bug (job 23959887): 1-edge samples were saved as object-dtype arrays; switched to `np.stack()` for uniform-length correspondences to emit regular int64 [E,N,2] arrays matching Breaking Bad convention.
  - Created `Puzzlefusion/scripts/eval_complex_oracle.slurm` (full pipeline, verifier.max_iters=6) and `eval_bones_retry.slurm`; added `extract_eval_metrics.sh` for parsing SLURM logs.
  - Ran Option 3 oracle evaluation across 5 datasets (bone_syn_pig, bone_syn_rib, fractura_ceramics, fractura_egg, fractura_real_bones); confirmed 0/27 bone samples converged through verifier sequential merge chain vs dramatic improvement on ceramics/egg.
  - Investigated GARF's GT information flow: confirmed `fracture_surface_gt`, graph adjacency, and `coarse_seg_pred` are loaded but never reach the denoiser (dead code in `denoiser_transformer.py` and `attention.py`); both GARF and PF++ share the reference-part GT anchor pose.
  - Applied 5 targeted edits to `GARF/GARF_vs_PuzzleFusion_comparison.md`: added GT anchor lines to GARF and PF++ setup sections, added "Shared GT Baseline — Reference Part Anchor" section, corrected caveat #2 to state only fracture-surface correspondences are asymmetric, updated Key Factors with verifier convergence analysis, updated conclusion noting dead code paths.

- what's next:
  - none

- blockers:
  - none
