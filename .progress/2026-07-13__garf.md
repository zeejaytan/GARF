# Progress log – 2026-07-13 – garf

## Entry 1 – 19:06
- timestamp: 2026-07-13 19:06
- branch/commit: main @ 2d489c0

- what changed:
  - Goal: read the Juglet-failure md files and experiment toward making GARF
    assemble the 9-piece Juglet. Read JUGLET_ROOTCAUSE_FINDINGS/PLAN, FINDINGS
    2026-05-28, DEPLOY_INFERENCE, CONVERGENCE_DIAGNOSIS, ARCHITECTURE, CLAUDE.md,
    SESSION_INSIGHTS. Prior state: pairwise perception failure established (Exp 6),
    relief/rim-oversampling/pseudo-GT all ruled out; mechanism carrier still open.
  - **Exp 10 (NEW, job 27188479) — looked INSIDE the frozen encoder for the first
    time.** `scripts/fracseg_introspection.py` + `exp10_fracseg_introspection.slurm`.
    The feature extractor is a FracSeg module whose coarse_segmenter head reads
    P(fracture) per point. Probe validated on labeled synthetic (AUC vs true GT
    0.95). Fracture-response (fired%): synthetic 9.7% / fresh real ceramics 3.4% /
    **Juglet 0.57%**. The encoder is essentially BLIND to Juglet's worn breaks —
    the direct feature-level cause of the perception failure. (Relief-band AUC was
    a red herring; fired% is the validated metric.)
  - **Exp 10b (job 27188744) — causal bridge.** Eroding fresh synthetic breaks
    toward worn drives fired% 9.8%→2.7% and AUC 0.91→0.66. Wear CAUSES the
    blindness. `exp10b_erosion_blindness.slurm`.
  - **Exp 11 (job 27189221) — remedy: fine-tune encoder on worn breaks.** New
    worn-break augmentation `assembly/data/breaking_bad/fracture_erosion.py`
    (+ params in base.py/module.py/breaking_bad.yaml, train-split only, default
    off). `scripts/finetune_frac_seg.py` + `exp11_finetune_frac_seg.slurm`:
    60ep, bone_synthetic 5 cats, erode prob 0.6, lr 2e-5. Result: Juglet fired%
    0.57→0.97 (+70%), synth AUC preserved (0.965), but control also rose to 6.0%
    so relative gap held — synthetic erosion is an imperfect proxy for real
    archaeological wear (as prior addenda predicted; relief already ruled out).
  - **Exp 12 (job 27189756, RUNNING) — the assembly test.** `scripts/
    merge_worn_encoder.py` swaps ONLY the encoder into GARF.ckpt; `exp12_juglet_
    assembly_worn.slurm` reruns the 3-seed pairwise oracle + symmetry-invariant
    chamfer + 9pc deploy. Pairwise evals done (~25s/seed); ICP chamfer scoring in
    progress (baseline to beat: true-mate 0.070 == non-mate 0.073, no separation).
  - Updated JUGLET_ROOTCAUSE_FINDINGS.md (Exp 10/10b/11 addenda) and the
    experiment-plan ledger. All heavy jobs run under SLURM; login node used only
    for light model-load / small-geometry smoke tests.

- what's next:
  - Read Exp 12 chamfer summary: did the worn encoder move true-mate vs non-mate
    separation vs the 0.070/0.073 baseline? If partial, consider a Juglet-domain
    self-supervised encoder adaptation (pseudo fracture labels from the relief
    detector on Juglet's own real worn geometry) as the next remedy lever.

- blockers:
  - none (Exp 12 ICP scoring is slow, ~30+ min CPU, but progressing)

## Entry 2 – 19:40
- timestamp: 2026-07-13 19:40
- branch/commit: main @ 2d489c0

- what changed:
  - **Exp 12 completed — encoder-only swap does NOT assemble the Juglet.** Worn
    encoder pairwise chamfer: true-mate 0.0705/0.0676 vs non-mate 0.0684/0.0658 —
    no separation (baseline 0.070/0.073). The +70% perception gain did not enable
    pairwise mating. Two reasons: Juglet's absolute response still ~6x below
    control, and swapping only the encoder feeds the denoiser off-distribution
    features (encoder-denoiser coupling). 9pc deploy GLB also written.
    Result: logs/diagnostics/pair_chamfer_juglet_worn_20260713_182732/summary.md.
  - Updated JUGLET_ROOTCAUSE_FINDINGS.md with the Exp 12 result + "bottom line"
    (mechanism nailed; synthetic-erosion remedy improves perception but is an
    imperfect proxy and, encoder-only, insufficient for assembly).
  - **Exp 13 designed + smoke submitted (job 27191385) — denoiser co-adaptation.**
    `exp13_denoiser_coadapt.slurm`: full denoiser fine-tune from GARF_worn_encoder
    .ckpt (worn frozen encoder + original denoiser) on worn-augmented bone_synthetic
    (frac_erode_prob 0.6, lr 1e-5), then eval on the Juglet pairwise chamfer oracle.
    Fixes the Exp 12 confound (denoiser never trained on worn-encoder features).
    Smoke (SMOKE=1, 1 epoch) queued to validate the strict finetuning load path
    before the full 40-epoch run.

- what's next:
  - When smoke passes, launch the full Exp 13 (EPOCHS=40) co-adaptation + Juglet
    eval. If pairwise chamfer still shows no mate/non separation, the honest
    conclusion is that real weathered training data is required (synthetic bone
    breaks -> ceramic Juglet transfer is the ceiling); document as the definitive
    finding and propose acquiring/annotating real worn archaeological breaks.

- blockers:
  - a100 partition busy (Exp 13 smoke pending on Priority)

## Entry 3 – 21:05
- timestamp: 2026-07-13 21:05
- branch/commit: main @ 2d489c0

- what changed:
  - Fixed a torch>=2.6 `weights_only` regression in `train.py`'s finetuning
    checkpoint load (local trusted Lightning ckpts carry OmegaConf hparams).
  - **Exp 13 completed (job 27192118) — denoiser co-adaptation: real but WEAK
    improvement.** Full denoiser fine-tune (40ep, lr 1e-5) from GARF_worn_encoder
    .ckpt on worn-augmented bone_synthetic (~24s/epoch), then Juglet pairwise
    chamfer. Result: true-mate mean 0.0645 vs non-mate 0.0671 — FIRST time true
    mates score better than non-mates; absolute true-mate chamfer 0.070→0.0645
    (~8%); best pair p0104 (Piece01-04) now 0.029 = control quality. BUT medians
    tied, most true mates still interleaved with non-mates (2nd-4th best are
    non-mates). GARF still cannot assemble the 9-piece Juglet.
    Result: logs/diagnostics/pair_chamfer_juglet_coadapt_20260713_194759/summary.md.
  - Pruned Exp 13 intermediate checkpoints (kept last.ckpt): reclaimed ~21.5GB.
  - Updated JUGLET_ROOTCAUSE_FINDINGS.md with the complete Exp 10-13 arc and the
    definitive bottom line: mechanism nailed (encoder blind to worn breaks),
    remedy direction works but is capped by the synthetic-bone -> real-ceramic
    domain proxy; remaining lever is real weathered supervision / Juglet-domain
    self-supervision.

- what's next:
  - Optional next lever (uncertain payoff, overfitting/circularity risk): Juglet
    self-supervised encoder adaptation using pseudo fracture labels from the
    validated relief-band detector on Juglet's OWN real worn geometry, to inject
    the real worn-ceramic texture the synthetic proxy lacks; then re-merge +
    co-adapt denoiser + re-score. Alternatively acquire annotated worn
    archaeological breaks (true mating correspondence) for proper fine-tuning.

- blockers:
  - Data ceiling: no real weathered/archaeological breaks with fracture labels or
    assembly GT available in-environment; synthetic bone-break wear is an
    imperfect proxy for real ceramic archaeological wear.
