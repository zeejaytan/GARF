# Progress log – 2026-05-28 – garf

## Entry 1 – 13:46
- timestamp: 2026-05-28 13:46
- branch/commit: main @ 2d489c0

- what changed:
  - Reconciled TORA paper claims vs default eval PNGs on Juglet; added `apply_rigid_transformations` to `TORA/repo/tora/procrustes.py` and `save_procrustes_assembly` path in `FlowVisualizationCallback` so `*_proposed_assembly*.png` is emitted alongside `*_generation*.png`.
  - Added TORA helper scripts `scripts/export_procrustes_assembly_viz.{py,sh}` and updated `TORA/eval_juglet_deploy.slurm` to enable the new visualization.
  - Fixed GARF deploy outputs in `assembly/models/denoiser/denoiser_base.py`: deploy GLB now uses `T_pred @ T_aug⁻¹` like benchmark mode, and the deploy JSON exports `gt_transform` (scatter aug) for downstream viz.
  - Updated `GARF/generate_viz.py` to reconstruct dataloader scatter aug via `--aug_seed`, apply the mesh-relative transform chain in `deploy_mode`, and relabel the proposed/predicted views in the HTML viewer.
  - Built compressed-local Juglet variant (`Dataset/Juglet_anchor_centered_local02`, alpha=0.2, max centroid spread 4.6740 → 0.9348) and packed HDF5 copies for both GARF (`input/juglet_deploy_local02.hdf5`) and TORA (`dataset/juglet_deploy_local02.hdf5`).
  - Added TORA config `config/data/zeroshot/juglet_deploy_local02.yaml` plus Slurm launchers `TORA/eval_juglet_deploy_local02.slurm` (job 25528198) and `GARF/infer_juglet_deploy_local02.slurm` (job 25528466); both completed.
  - Wrote `GARF/scripts/garf_matched_diagnostic.py` + `diagnose_garf_matched_9pc.slurm`; matched 9-piece run (job 25531535) produced `logs/diagnostics/matched_runs/diag_20260528_132826/{selected_samples.json,diagnostic_summary.csv,diagnostic_summary.md}`.
  - Key diagnostic finding: `juglet_raw` and `juglet_local02` give identical GARF trajectories (per-part recenter + random rotate + unit scale strips global layout); GARF's predicted translation spread (~0.69) is in-distribution for BB/pig (~0.5–1.0) but 6–7× too small for Juglet's 4.7m scan spread. local02 helps TORA because its flow target depends on mesh layout; GARF is unaffected.
  - Saved consolidated writeup to `GARF/JUGLET_FINDINGS_2026-05-28.md` (PNG glossary, local02 experiment, matched-9pc diagnostic table, smoking-gun analysis, and three options to improve GARF on Juglet).

- what's next:
  - none

- blockers:
  - none
