# Progress log – 2026-04-04 – garf

## Entry 1 – 22:17
- timestamp: 2026-04-04 22:17
- branch/commit: main @ 2d489c0

- what changed:
  - Ran 6-dataset complex evaluation via `eval_complex.slurm` (job 23583868): bone_synthetic/pig (78.1% acc), bone_synthetic/rib (63.8%), fractura_real/ceramics (56.3%), fractura_real/egg (26.1%), fractura_real/bones (58.3%), tray_archaeological failed due to HDF5 data_split format
  - Fixed `tray_archaeological.hdf5`: data_split stored scalar string instead of array, and sample paths missing `artifact/` prefix. Fixed both issues in-place with h5py.
  - Reran tray eval (job 23583949): 40-fragment tray got 2.5% part_acc / 84.8° RMSE — expected given no GT assembly exists
  - Generated visualizations for all 6 datasets: `viz_output/bone_syn_pig/`, `bone_syn_rib/`, `fractura_ceramics/`, `fractura_egg/`, `fractura_bones/`, `tray_archaeological/`
  - Deep investigation into GARF test_step GT usage: traced every line where GT is used in `denoiser_base.py:304-625`. Confirmed GT only used for anchor pinning (coordinate frame), metrics, and visualization — never enters the denoiser network.
  - Created `GARF_ARCHITECTURE.md` — comprehensive reference documenting inference pipeline, exhaustive GT usage map, quaternion conventions, HDF5 formats, data pipeline, and how to run inference without GT
  - Updated memory files: `project_tray_dataset.md` (tray is user's data, scanned positions, no GT), `feedback_garf_model.md` (GARF is generalizing reassembly-by-pose-inference, not just pose regression)

- what's next:
  - Build inference-only pipeline for tray_archaeological (no GT, pick anchor sherd, skip random scatter)

- blockers:
  - none
