# Progress log – 2026-04-15 – garf

## Entry 1 – 17:12
- timestamp: 2026-04-15 17:12
- branch/commit: main @ 2d489c0

- what changed:
  - Discussed methodology for Option 3 (oracle matching data) with user; clarified that correspondence ≠ assembly solution but oracle correspondences are an upper-bound, not a fair head-to-head vs GARF
  - Wrote `/Puzzlefusion/generate_oracle_matching_data.py`: combined script that (1) assembly-normalises converted .npz so combined cloud fits [-0.5, 0.5] — required for verifier's hardcoded Chamfer bins — and (2) generates oracle matching_data from GT geometry (critical points via ε-nearest-neighbour, correspondence pairs via nearest-crit matching)
  - Generated matching data for all 5 datasets into `data/matching_data_oracle/<dataset>/*.npz` and normalised pc_data into `data/pc_data_norm/<dataset>/val/`. Edge coverage: synthetic 15–40%, real 39–100%
  - Wrote `/Puzzlefusion/scripts/eval_complex_oracle.slurm` running full pipeline (denoiser + verifier, `max_iters=6`) on all 5 datasets
  - Submitted SLURM job 23959887 on gpu-h100 — 4/5 datasets succeeded, fractura_real_bones crashed with DataLoader collate TypeError on object-dtype correspondence arrays in 1-edge (2-part) samples
  - Fixed generator: 1-edge samples now saved as regular int64 [1, N, 2] arrays (matches Breaking Bad convention). Regenerated bones matching data; submitted SLURM 23960250 which completed in 44s
  - Dramatic oracle results: ceramics 21.5%→90.4%, egg 26.1%→100%, bones 46.9%→58.3% (ties GARF exactly on all 4 metrics). Synthetic pig/rib still lag GARF even at oracle ceiling
  - Rewrote `/GARF/GARF_vs_PuzzleFusion_comparison.md` with full three-way comparison table, oracle-matching-data explanation section, per-dataset analysis, and caveats emphasising this is a ceiling not a deployment estimate
  - Added `/Puzzlefusion/scripts/extract_eval_metrics.sh` helper to parse SLURM log metrics

- what's next:
  - none

- blockers:
  - none
