# @package _global_

defaults:
  - override /model: denoiser_diffusion_quaternion
  - override /loggers: wandb

project_name: 3D Assembly Denoiser
experiment_name: denoiser_diffusion_${now:%Y-%m-%d_%H-%M-%S}
tags: ["hpc", "denoiser", "diffusion", "quaternion"]

trainer:
  check_val_every_n_epoch: 50
  max_epochs: 1500