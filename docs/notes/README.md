# Notes and experiment write-ups

Research notes, session insights, and experiment plans for the Spartan GARF work. Kept out of the repo root so the working tree stays scannable.

Slurm job scripts live in [`../slurm/`](../slurm/). Helper Python for bone/tray viz lives under [`../scripts/`](../scripts/). Pipeline entrypoints that Slurm/docs call as `python <name>.py` from the repo root stay at the root (`generate_viz.py`, `create_juglet_hdf5.py`, etc.).
